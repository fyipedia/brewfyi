# Smithery configuration for brewfyi
# https://smithery.ai/docs/build/project-config
startCommand:
  type: stdio
  configSchema:
    type: object
    required: []
    properties: {}
  commandFunction: |-
    config => ({ command: 'uvx', args: ['--from', 'brewfyi[mcp]', 'python', '-m', 'brewfyi.mcp_server'] })
